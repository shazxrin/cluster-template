apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    app.kubernetes.io/name: notifier
    app.kubernetes.io/part-of: argocd
  name: notifier
  namespace: argocd
spec:
  project: default
  source:
    repoURL: {{ cluster_repository_url }}
    targetRevision: HEAD
    path: notifier
    directory:
      recurse: true
      exclude: "{notifier-app-configuration-secret.yaml}"
  destination:
    server: https://kubernetes.default.svc
    namespace: notifier
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: true
    syncOptions:
      - CreateNamespace=true
