apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    app.kubernetes.io/name: alif
    app.kubernetes.io/part-of: argocd
  name: alif
  namespace: argocd
spec:
  project: default
  source:
    repoURL: {{ cluster_repository_url }}
    targetRevision: HEAD
    path: alif
    directory:
      recurse: true
      exclude: "{alif-app-configuration-secret.yaml}"
  destination:
    server: https://kubernetes.default.svc
    namespace: alif
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: true
    syncOptions:
      - CreateNamespace=true
